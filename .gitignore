# Сборки и зависимости
node_modules/

# Внутренние документы (спеки)
docs/

# Артефакты сборки electron-builder
/test direct/dist/
/test direct/dist2/

# Готовый бинарник: раздаётся через GitHub Releases, а не через репозиторий
# (в истории он остаётся — .git уже весит ~69 МБ; чистить только через
# git filter-repo + force-push, что сломает существующие клоны)
/RuleFlowEditor.exe

# Личные данные и тестовые файлы (не для публичного репозитория)
/1.json
/rules2.json
/rule-new.txt
/incy-routing-link.txt
/*.dat
/test direct/1.txt
/test direct/.claude/